export const metadata = {
  title: 'Adobe Stock SEO Metadata Generator',
  description: 'Generate titles, descriptions, and keywords for Adobe Stock',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
